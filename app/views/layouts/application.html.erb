<!DOCTYPE html>
<html class="h-full bg-gray-100">
  <head>
    <title>您的应用名称</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full">
    <div class="min-h-full">
      <nav class="bg-gray-800">
        <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
          <div class="flex h-16 items-center justify-between">
            <div class="flex items-center">
              <div class="flex-shrink-0">
                <%= link_to root_path, class: "text-white font-bold text-xl" do %>
                  您的应用名称
                <% end %>
              </div>
            </div>
            <div class="hidden md:block">
              <div class="ml-4 flex items-center md:ml-6">
                <% if user_signed_in? %>
                  <%= button_to '登出', destroy_user_session_path, method: :delete, class: "rounded-md bg-gray-700 px-3 py-2 text-sm font-medium text-white hover:bg-gray-600" %>
                <% else %>
                  <%= link_to '登录', new_user_session_path, class: "rounded-md bg-gray-700 px-3 py-2 text-sm font-medium text-white hover:bg-gray-600 mr-2" %>
                  <%= link_to '注册', new_user_registration_path, class: "rounded-md bg-gray-700 px-3 py-2 text-sm font-medium text-white hover:bg-gray-600" %>
                <% end %>
              </div>
            </div>
          </div>
        </div>
      </nav>

      <main>
        <div class="mx-auto max-w-7xl py-6 sm:px-6 lg:px-8">
          <% if notice %>
            <p class="rounded-md bg-blue-50 p-4 text-sm text-blue-700 mb-4"><%= notice %></p>
          <% end %>
          <% if alert %>
            <p class="rounded-md bg-red-50 p-4 text-sm text-red-700 mb-4"><%= alert %></p>
          <% end %>

          <%= yield %>
        </div>
      </main>
    </div>
  </body>
</html>